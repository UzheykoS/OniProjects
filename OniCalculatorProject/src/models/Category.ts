import { Status }  from './Status'

export default class Category {
    id: number;
    name: string; 
    status: Status;

    constructor() {
    }

    // constructor(name: string, id: number) {
    //     this.Name = name;
    //     this.Id = id;
    // }
}